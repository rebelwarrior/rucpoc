- provide(:title, "Facturas")
%h1 Factura:
.row
  %aside.span4
    %section
      %h1
        -if @debtor
          = @debtor.name
          ="   |  Persona Contacto: "
          = @debtor.contact_person
        /end  
      %p
        - if @debtor and @debtor.collections.any?
          %h3
            Facturas:
          %ol.microposts
            = render @collections #renders the partial
        /end