-#%li{:'data-toggle' => 'popover', :'data-original-title' => "#{log.user_id} hace #{time_ago_in_words(log.created_at)} escribió: ", :'title data-content' => "#{log.content}", :'data trigger' => 'hover', :'data container' => 'sidebar'}
-#  = log.content
-#  %br
-#  = "#{find_log_user_email log} escribió en #{l log.created_at.in_time_zone("America/Puerto_Rico") }"#.to_formatted_s(:long_ordinal}"
.accordion-group
  .accordion-heading
    %a.accordion-toggle{:'data-toggle' => "collapse", :'data-parent' => '#accordion2', :href => "#collapse#{log.id}"}= "Bitácora #{log.id} de la factura #{log.collection_id}"
    %div{ :id => "collapse#{log.id}", :class => "accordion-body collapse"}
      .accordion-inner
        = log.content
        .grey
          = "#{find_log_user_email log} escribió en #{l log.created_at.in_time_zone("America/Puerto_Rico") }"#.to_formatted_s(:long_ordinal}"
          
-# to have it stay open have "accordion-body collapse in"