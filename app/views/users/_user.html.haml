%li
  = link_to user.email, user
  - if current_user.admin? && (not current_user?(user))
    = " | "
    = link_to "delete", user, method: :delete, data: {confirm: "Confirme Borrar Record: ¿Seguro?"}
  /  